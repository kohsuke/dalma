/**
 * Dalma workflow engine API.
 *
 * <p>
 * This package hosts APIs that are accessed by applications that
 * use Dalma engine.
 *
 * <p>
 * {@link EngineFactory} is the entry point to the API. It creates
 * {@link Engine} instance, and then the rest of interfaces are
 * hooked from there. 
 */
package dalma;